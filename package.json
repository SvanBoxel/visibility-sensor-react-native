{
  "name": "inviewport-react-native",
  "version": "0.1.0",
  "description": "React Native component that helps with determining whether a component is in the viewport.",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "echo \"Todo: specify tests\" && exit 0",
    "build": "tsc",
    "build:watch": "tsc -w",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "lint": "prettier -c --config .prettierrc 'src/**/*.{ts,tsx}'",
    "lint:fix": "prettier --config .prettierrc 'src/**/*.{ts,tsx}' --write",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "React Native",
    "React",
    "Viewport"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/react-native": "^0.62.14",
    "react": "^16.9.0",
    "react-dom": "^16.13.1",
    "react-native": "^0.62.2"
  },
  "devDependencies": {
    "@types/node": "14.0.6",
    "prettier": "^2.0.5",
    "typescript": "^3.9.3"
  },
  "files": [
    "dist/**/*"
  ]
}
